# AI Assistant Context

This is a native SwiftUI iOS application built with Bazel.

## Build & Run

```bash
bazel build //modules/test_valdi:DatesApp
bazel run //modules/test_valdi:DatesApp
```

Uses `rules_swift` and `rules_apple` for iOS development.
